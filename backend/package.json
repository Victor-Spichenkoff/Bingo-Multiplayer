{
  "name": "backend",
  "version": "1.0.0",
  "description": "http://localhost:3001/users para acessar, npm start(pasta backend) para iniciar o server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
